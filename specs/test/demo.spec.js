const assert = require('assert');
describe('Мой демо тест', () => {
     it('Мой демо тест', () => {
                const a = 1;
				const b = 2;
                assert.deepEqual(a+b, 3)

            }
        )
    }
)
